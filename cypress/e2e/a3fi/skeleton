CRUD Usuários
CRUD Pessoas
CRUD Propriedades Rurais
    CRUD Caixa (CRUD de transações[Rceita/Despesa])
    CRUD Talhões (CRUD de Lotes e pastagem)
        CRUD Atividades (CRUD de atividades do talhão/lote)
        CRUD Produções (CRUD de produções )
        CRUD Medição de altura do pasto (CRUD para cadastro das medições do pasto [talhão/lote])       
    CRUD Patrimônios (CRUD de Patrimônios da fazenda e CRUD de Animais [animais é tratado com Patrimônio também])
    CRUD Locais (CRUD de Locais)

Configurações
    CRUD Menus
    CRUD Perfis
        Adicionar Permissão
        Remover Permissão
    CRUD Tipos de Produto
    CRUD Espécies de Vegetação
    CRUD Tipos de Atividade
    CRUD Espécies
    CRUD Raças
    CRUD Tipos de Locais
    CRUD Unidades de medida
    CRUD Tipos de Transação
    CRUD Medidas de trabalho
    CRUD Tipos de Patrimônio
    CRUD Tipos de Manejo Animal