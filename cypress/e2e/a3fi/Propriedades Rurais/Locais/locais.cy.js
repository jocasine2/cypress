describe('Funcionalidade de Locais', () => {
    it('Deve listar todos os locais cadastrados', () => {
      // Passos para testar a listagem de locais
    });
  
    it('Deve permitir a criação de um novo local', () => {
      // Passos para testar a criação de um novo local
    });
  
    it('Deve permitir a edição de um local existente', () => {
      // Passos para testar a edição de um local existente
    });
  
    it('Deve permitir a exclusão lógica de um local', () => {
      // Passos para testar a exclusão lógica de um local
    });
  
    it('Deve exibir mensagens de validação ao tentar criar ou editar com dados inválidos', () => {
      // Passos para testar as mensagens de validação
    });
  });
  